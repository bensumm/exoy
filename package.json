{
  "name": "exoy",
  "private": true,
  "version": "0.1.0",
  "description": "Exoy LED controller — web app (PWA) + local bridge",
  "workspaces": [
    "packages/protocol",
    "packages/bridge",
    "packages/app"
  ],
  "scripts": {
    "build": "npm run build --workspace @exoy/protocol && npm run build --workspace @exoy/bridge",
    "test": "npm run test --workspace @exoy/protocol",
    "dev:bridge": "npm run dev --workspace @exoy/bridge",
    "dev:app": "npm run dev --workspace @exoy/app"
  },
  "engines": {
    "node": ">=20"
  }
}
